package by.milavitsky.homework.task3_2.creator;

import by.milavitsky.homework.task3_1.view.Message;
import by.milavitsky.homework.task3_2.entity.JaggedArray;
import static by.milavitsky.homework.task3_2.validation.Validator.*;

import java.util.ArrayList;
import java.util.Scanner;

public class CreatorArray {

    /*public static int creatSizeArrayA() {
        Scanner scanner = new Scanner(System.in);
        Message.showMessage("Enter the length of array A");
        int lengthA;
        while((lengthA = scanner.nextInt()) <= 0 & (lengthA = scanner.nextInt()) < 6 ){
            Message.showMessage("Enter the length of array (0 - 5) A");
        }


        return lengthA;
    }
    public static ArrayList<Integer> creatSizeArrayB(int sizeA) {
        Scanner scanner = new Scanner(System.in);
        int lengthB = 0;// проверку на прямоугольную
        ArrayList <Integer> list = new ArrayList<Integer>();
        Message.showMessage("Enter the length of array B");
        for (int i = 0; i <sizeA; i ++){
            while((lengthB = scanner.nextInt()) <= 0 & (lengthB = scanner.nextInt()) < 11){
                Message.showMessage("Enter the length of array B (0 -10)");
            }
            list.add(lengthB);
        }
        if(isArrayRectangular(list)){
            Message.showMessage("Massive must be not rectangular ");
        }
        return list;
    }
    public static int creatArray() {
        Scanner scanner = new Scanner(System.in);
        Message.showMessage("Enter value");
        int value = scanner.nextInt();
        return value;
    }*/


}
